import { describe, it, expect } from "vitest";

describe("Example test", () => {
  it("should work", () => {
    expect(1 + 1).toBe(2);
  });
});

describe("Alap teszt", () => {
  it("Egyszerű összeadás", () => {
    expect(1 + 1).toBe(2);
  });
});
